import { ModuleWithProviders } from '@angular/core';
import { Routes,RouterModule } from '@angular/router';

const manageRoutes: Routes = [
 
];
export const manageRouting: ModuleWithProviders = RouterModule.forChild(manageRoutes);


